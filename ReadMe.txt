1. Python 2.7 (Anaconda 2 x64) 'Spider' environment
2. OS: Windows 7 x64
3. Libraries Used:
	a) datetime
	b) pandas
	c) pandas_datareader (only this one requires installation):
	   Anaconda Prompt: pip install pandas-datareader
	d) numpy
	e) pylab
	f) scipy

4. To run program:
	a) Open a prompt (a Windows 'cmd' ): start > run > "cmd" 
	b) Navigate to where the python file is, using the commands 'cd' (change directory)
	   and 'dir' (to show files in the directory, to verify your head). For our example something like,
	c) Run the program: python Panko_Aliaksandr-Project_1-Nov_13_2017.py

5. Alternative way: just open the file from a Spider and press F5




